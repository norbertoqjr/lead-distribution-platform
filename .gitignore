node_modules/
dist/
build/
.next/
coverage/

# Deliverable apps — each is its own repository, tracked separately
/web/
/api/

# Local tooling — not part of the deliverable
.claude/

# Secrets — never commit
.env
.env.*
!.env.example
*.local.md
credentials.local*

.DS_Store
*.log
